version: "3.8"
services:
  cybersecurity-toolkit:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ollama_data:/data/ollama
    network_mode: host
    restart: always

volumes:
  ollama_data:
